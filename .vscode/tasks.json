{
    "tasks": [
        {
            "type": "shell",
            "label": "Make",
            "command": "make",
            "options": {"cwd": "${workspaceFolder}/build"},
        }
    ],
    "version": "2.0.0"
}